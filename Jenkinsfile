pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh "pwd"
                dir('your-sub-directory') {
                    sh "ls"
    }
            }
        }
    }
}
